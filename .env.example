# Docker Compose Environment Variables
# Copy this to .env and customize for your environment

# MongoDB Configuration (if using local MongoDB instead of Atlas)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change_this_password_in_production
MONGO_DATABASE=inventory_system
MONGO_PORT=27017

# Backend Configuration
BACKEND_PORT=3001

# Frontend Configuration
FRONTEND_HTTP_PORT=80
FRONTEND_HTTPS_PORT=443
VITE_API_BASE_URL=http://10.0.10.141:3001/api

# Redis Configuration
REDIS_PORT=6379
REDIS_PASSWORD=change_this_redis_password

# Notes:
# 1. For MongoDB Atlas: Configure MONGODB_URI in inventory-backend/.env.local
# 2. For local MongoDB: Use the settings above
# 3. Make sure to change all passwords before deploying to production
# 4. Update VITE_API_BASE_URL with your actual server IP or domain
